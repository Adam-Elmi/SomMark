# header

The accumulated string of all header content (meta tags, styles, etc.).

**Syntax:** `mapper.header` (String)

```javascript
console.log(mapper.header);
// <meta ...><style>...</style>...
```
