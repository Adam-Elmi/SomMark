# styles

An array containing all registered custom CSS strings.

**Syntax:** `mapper.styles` (Array\<string>)

```javascript
mapper.addStyle(".text { color: red; }");
console.log(mapper.styles[0]);
```
