# loadCss

Asynchronously loads a CSS file from a path and adds it to the styles.

**Syntax:** `mapper.loadCss(path)`

-   **path** (string): The file path (relative or absolute).
-   **Returns**: `Promise<void>`

```javascript
await mapper.loadCss("./styles/theme.css");
```
