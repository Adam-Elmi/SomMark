# todo

Parses a todo item state.

**Syntax:** `mapper.todo(checked)`

-   **checked** (string | boolean): "x", true (checked), or "-", false (unchecked).
-   **Returns**: `true` or `false`.

```javascript
const isDone = mapper.todo("x"); // true
const isPending = mapper.todo("-"); // false
```
