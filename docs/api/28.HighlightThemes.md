# HighlightThemes

Returns a list of all available Highlight.js themes.

**Syntax:** `mapper.HighlightThemes` (Array\<string>)

**Note:** Returns an empty array if highlighting is disabled.

```javascript
console.log(mapper.HighlightThemes);
// ["atom-one-dark", "github", ...]
```
