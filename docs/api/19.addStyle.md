# addStyle

Registers custom CSS.

**Syntax:** `mapper.addStyle(css)`

-   **css** (string | object): Raw CSS or object-style rules.

```javascript
// String
mapper.addStyle(".btn { background: blue; }");

// Object
mapper.addStyle({
  ".btn": { background: "blue" }
});
```
