# enable_table_styles

Boolean flag. If `true`, default CSS styles for tables are added automatically.

**Syntax:** `mapper.enable_table_styles` (Boolean)

Default: `true`.

```javascript
// Disable default table styling
mapper.enable_table_styles = false;
```
