# enable_highlightTheme

The `enable_highlightTheme` property controls whether the selected highlight theme CSS is automatically injected into the generated HTML.

**Type:** `Boolean`

**Default:** `true`

### Example

```javascript
import { HTML } from "sommark";

// Disable auto-injection of highlight styles
HTML.enable_highlightTheme = false;
```
