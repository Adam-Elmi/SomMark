# get

Get the mapping object for a specific tag ID.

**Syntax:** `mapper.get(id)`

-   **id** (string): The identifier to search for.
-   **Returns**: The mapping object or `null`.

```javascript
const mapping = mapper.get("Block");
if (mapping) {
  console.log("Block is registered!");
}
```
