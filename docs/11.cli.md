# SomMark CLI Guide

The **SomMark CLI** allows you to compile `.smark` files into structured output formats directly from the terminal.

Supported output targets:

* **HTML**
* **Markdown**
* **MDX**
* **JSON**
* **Plain Text**

---

## Installation

You can run SomMark without installation using `npx`, or install it globally.

```bash
# Option 1: Run instantly (no install)
npx sommark [command]

# Option 2: Install globally
npm install -g sommark
```

After global installation:

```bash
sommark --version
```

---

## Command Structure

```
sommark [output-flag] [input-file] [options]
```

You must specify exactly one output format flag.

---

## 1. Basic Usage

### Convert to HTML

```bash
sommark --html index.smark
```

### Convert to Markdown

```bash
sommark --markdown index.smark
```

### Convert to MDX

```bash
sommark --mdx index.smark
```

### Convert to JSON

```bash
sommark --json index.smark
```

### Convert to Plain Text

```bash
sommark --text index.smark
```

---

## 2. Introspection & Debugging

### Inspect Tokens (Lexer)

```bash
sommark --lex index.smark
```

Prints the token stream generated by the lexer.

### Inspect AST (Parser)

```bash
sommark --parse index.smark
```

Prints the Abstract Syntax Tree (AST) generated by the parser.

### List Plugins

```bash
# List all plugins
sommark list plugins

# List only internal (built-in) plugins
sommark list plugins --internal
# or
sommark list plugins -i

# List only external (user-defined) plugins
sommark list plugins --external
# or
sommark list plugins -e
```

Lists all enabled plugins and their origin (internal/external), author, and description.

### Show Execution Pipeline

```bash
sommark list pipeline
```

Displays the order in which plugin phases will execute.

### Show Current Configuration Data

```bash
sommark show config
```

Displays the resolved configuration data (from the used configuration file).

### Show Configuration Path

```bash
sommark show --path-config
```

Displays the absolute path of the configuration file currently in use.

### Color Management

```bash
sommark color on
sommark color off
```

Enables or disables colored output in the CLI.

---

## 3. Output Behavior

### Default Behavior

If no output path is specified:

* Output file is created
* In the same directory as the input file
* With the corresponding extension

Example:

```
index.smark → index.html
```

---

### Specify Output File and Directory

Use `-o` to define the output file name and optionally a directory.

```bash
sommark --html src/page.smark -o page.html ./dist
```

This will:

* Generate `page.html`
* Place it inside `./dist`

If the directory does not exist, it must be created beforehand.

---

### Print to Console (stdout)

Use `-p` to print the result instead of writing to a file.

```bash
sommark --html -p src/page.smark
```

Useful for:

* Piping output
* Debugging
* CI workflows

Example with piping:

```bash
sommark --html -p page.smark > output.html
```

---

## 3. Using a Configuration File

You can define custom mappers and behavior inside:

```
smark.config.js
```

The CLI automatically loads this file when it exists in the working directory.

### Example Configuration

```javascript
import { Mapper } from "sommark";

const Helper = new Mapper();

Helper.register("Block", ({ content }) => {
  return content;
});
```

Then simply run:

```bash
sommark --html page.smark
```

The CLI will use the custom mapper during compilation.

---

## 4. Example Workflow

### Project Structure

```
project/
 ├─ page.smark
 ├─ smark.config.js
 └─ dist/
```

### Compile

```bash
sommark --html page.smark -o page.html  ./dist
```

> [!Important]
> You cannot do `sommark --html page.smark -o ./dist/page.html` as it will create `./dist/page.html.html`. So separately define the directory and the file name. For example: `sommark --html page.smark -o page.html ./dist`.

### Result

```
project/dist/page.html
```

---

## 5. CLI Flags Reference

| Flag         | Short | Description                                       |
| ------------ | ----- | ------------------------------------------------- |
| `--help`     | `-h`  | Display help menu.                                |
| `--version`  | `-v`  | Show installed version.                           |
| `--html`     | —     | Output HTML file.                                 |
| `--markdown` | —     | Output Markdown file.                             |
| `--mdx`      | —     | Output MDX file.                                  |
| `--json`     | —     | Output JSON file.                                 |
| `--text`     | —     | Output plain text file.                           |
| `--lex`      | —     | Output lexer tokens to console.                   |
| `--parse`    | —     | Output parser AST to console.                     |
| `--internal` | `-i`  | Filter internal plugins (for `list`).             |
| `--external` | `-e`  | Filter external plugins (for `list`).             |
| `-o`         | —     | Specify output filename and optionally directory. |
| `--print`    | `-p`  | Print output to console (stdout).                 |

---

## 6. Commands Reference

| Command          | Description                                    |
| ---------------- | ---------------------------------------------- |
| `list plugins`   | List all enabled plugins.                      |
| `list pipeline`  | Show plugin execution order.                   |
| `show config`    | Show resolved configuration data.              |
| `show --path-config` | Show resolved configuration file path.     |
| `color on/off`   | Enable or disable CLI colors.                  |
| `init`           | Initialize a global configuration directory.   |

---

## 6. Best Practices for Using SomMark CLI

* **Keep your config file in the project root**
  Place `smark.config.js` in the same folder as your `.smark` files so the CLI can automatically find it.

* **Use `-p` to print output**
  This shows the result in the terminal instead of creating a file. Useful for testing, automation, or CI pipelines.

* **Specify output paths**
  When building for production, always give an output file and folder using `-o` to avoid overwriting files.

* **Use only one output format at a time**
  Don’t combine `--html` and `--markdown` in a single command—pick one.

* **Check your `.smark` files first**
  Make sure your files have proper Blocks, Inlines, and Atblocks before compiling to avoid errors.



