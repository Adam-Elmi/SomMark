# Raw Content Plugin Complex Test

[Raw]
  This content should be escaped:
  Brackets: [ ]
  Backslash: \
  At-tokens: @_ _@
  Args-chars: = : , ( )
  
  [NestedButShouldBeEscaped] - This should stay as text.
[end]

[MDX = "component"]
  import { Button } from './ui';
  
  <Button onClick={() => console.log("clicked")}>
    Escaped: [ ] \ @_
  </Button>
[end]

[Code = "javascript"]
  function test() {
    const arr = [1, 2, 3];
    const obj = { key: "val" };
    if (true && !false) {
      return (a, b) => a + b;
    }
  }
[end]
